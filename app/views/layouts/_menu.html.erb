<nav class="navbar navbar-expand-lg navbar-light bg-light me-auto">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag "icvLogoSquare.png", size: "40" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to 'Articles', articles_path,
                        class: "btn btn-outline-primary my-2 my-sm-0 mx-2" %>
          <% end %>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-outline-dark my-2 my-sm-0" %>
          <% else %>
            <%= link_to 'Sign In', new_user_session_path, class: "btn btn-outline-success my-2 my-sm-0 mx-2" %>
            <%#= link_to 'Sign Up', new_user_registration_path, class: "btn btn-outline-success my-2 my-sm-0 mx-2" %>
          <% end %>
        </li>
        <li class="nav-item">
          <% if current_user.try(:admin?) %>
            <%= link_to 'Admin', "/admin", class: "btn btn-outline-danger my-2 my-sm-0 mx-2" %>
          <% end %>
        </li>

      </ul>

    </div>
  </div>
</nav>

